{% extends 'base.html.twig' %}

{% block title %}Job Details{% endblock %}

{% block main %}
    <main class=" container d-flew flex-row justify-content-evenly ">
        <div class="card w3-theme-l5 ">
            <div class="card-body d-flex flex-column">
                <div class="card-title" >{{ "🚀" ~ job.position }}</div>
                <span class="text-muted font-italic ">{{ jobTimes }}</span>
                <div class="tags mb-2 mt-1">
                    <span class="badge bg-secondary">{{ job.employmentType }}</span>
                    {#  <span class="badge bg-primary">{{ job.category }}</span> #}
                </div>
                <ul class="list-group list-group-flush ">
                    <li class="list-group-item w3-theme-l5">{{ "🧱" ~ job.entreprise }}</li>
                    <li class="list-group-item w3-theme-l5">{{ "📍" ~ job.location }}</li>
                </ul>


                <p class="card-text">{{  job.description }}</p>

                {% if (app.user==null) or is_granted("ROLE_JOBSEEKER") %}
                    <a href="{{ path('app_apply', { 'job': job.id }) }}" class="btn btn-primary w3-theme-d4 align-self-end mt-auto">Apply</a>
                {% endif %}

                </div>
        </div>
    </main>
{% endblock %}
